# 影片分析

> 建立於 2026-09-10 · **第一期已上線**（www.hohosports.com）· 第二期未動工
>
> 這份檔案記錄「學生與教練上傳跑步／重量訓練影片」這個功能的技術決策、
> 上線流程與費用。日後開發這一塊時以此為基礎；決策改變時請直接改這份檔案，
> 並在文末的「決策紀錄」補一行。
>
> 目前狀態一覽：
>
> | | 狀態 |
> |---|---|
> | 第一期（上傳／影片庫／播放器／紀錄／並排比對） | ✅ 上線 |
> | 分析工具（計時、數步、劃線）與放大 | ✅ 上線 |
> | Cloudflare R2 儲存 | ✅ 正式站已切換 |
> | `purge_videos` 保留期限清理 | ⚠️ 指令寫好了，**還沒排程**，目前要手動跑 |
> | 第二期（姿態估計 + AI 回饋） | ⬜ 未動工 |

---

## 一、結論先講

**分兩期做，第一期不碰 AI。**

- **第一期**：純影片平台——上傳、影片庫、播放器（變速／逐格）、教練在時間點留批註、並排比較。這已經是 Hudl、OnForm 這類專業教練平台的核心功能，本身就是完整可用的產品。
- **第二期**：在第一期累積的影片上加姿態估計（pose estimation），算出真實的關節角度與時間序列，再交給 Claude 寫成教練語言的回饋。

**為什麼先做第一期**：如果教練用一個學期後發現手動逐格看片加批註已經夠用（很常見的結果），第二期的月費與約一千行程式碼就省下來了；如果教練說「我想要膝角數字」，那時已經有一整個學期的真實影片可以驗證第二期做得準不準。

---

## 二、已排除的方案：抽格丟給 Claude

最初評估的做法是「影片 → ffmpeg 抽 12–16 張關鍵格 → 當成多張圖送 Claude 分析」。

**已排除。** 原因有二：

1. 產品觀感上不夠專業。
2. 成本反而更高——16 張圖約 25k input tokens（每條約 US$0.12），而第二期送「關節角度數字」只要約 2k tokens（每條約 US$0.03）。

**但要記住這個事實**：沒有任何 LLM API 吃原始影片，Messages API 只接受圖片（jpeg/png/webp/gif）與 PDF，沒有 video 這種 content block。市面上所有「AI 影片分析」產品底層都是抽格。所以「不抽格」等於「不用 LLM 直接看動作」，這正是第二期改走 pose estimation 的理由——它不是抽格的簡化版，而是更專業的版本。

---

## 三、第一期：純影片平台

### 3.1 三個必須解決的技術問題

#### （1）檔案放哪——不能放 Render 容器

`render.yaml` 目前是 `plan: starter` 且**沒有掛 disk**，容器檔案系統每次部署就重置。

| | Render Disk | 物件儲存（Cloudflare R2） |
|---|---|---|
| 改動 | 掛 disk + 設 `DJANGO_MEDIA_ROOT` | 加 `django-storages` + `boto3`，改 `config/settings.py` 的 `STORAGES` |
| 儲存費 | 約 US$0.25/GB/月 | 約 US$0.015/GB/月 |
| 流量費 | 含在服務內 | **$0（R2 不收 egress）** |
| 代價 | 服務鎖死單一 instance、失去 zero-downtime deploy | 多一個外部服務要管 |

**決定：用 Cloudflare R2。** 它 S3 相容，`STORAGES` 換個 backend 即可；`MEDIA_ROOT` 那幾行留著給本機開發。影片每次播放都是 egress，R2 免流量費是關鍵。

#### （2）上傳不能走 gunicorn

`render.yaml` 的 `startCommand` 是 `--timeout 120`。一條 100MB 的片經 Django 上傳會超時，而且整個檔案會佔住 worker 記憶體。

**做法：前端直傳。** Django 只發一個 presigned PUT URL → 瀏覽器用 `fetch` 把片直接送去 R2 → 成功後才 POST 一筆 metadata 回 Django。這樣 gunicorn 永遠只處理幾 KB 的 JSON。

#### （3）格式相容性——這個一定會咬到

iPhone 預設拍 HEVC 的 `.mov`，Chrome / Android 播不了。三個選項：

- 瀏覽器端先轉碼成 H.264 MP4（免費，吃學生手機效能）← **先用這個**
- 上傳頁明確要求學生改用「最相容」拍攝設定（免費，但靠自律）
- 後端排隊跑 ffmpeg 轉檔（Render background worker 約 US$7/月）

這是最容易被忽略、上線後最多人投訴的一點。

### 3.2 功能範圍

- 上傳（直傳 R2）
- 影片庫：按運動員 / 日期 / 關聯項目瀏覽
- 播放器：變速播放、逐格前進後退
- 教練批註：綁在**指定時間點**上的文字註解
- 並排比較：同畫面比對兩條片（例如本月 vs 三個月前）

上線後又加了（見 3.6）：放大、A→B 計時、數步、在影片上劃線，
量出來的東西存成「分析重點紀錄」。

### 3.3 現有地基

這些已經在專案裡，可以直接沿用或參照：

| 已有的東西 | 位置 | 用途 |
|---|---|---|
| 「上傳檔案 → 外部 API → structured JSON → 存進 model」樣板 | `nutrition/vision.py` | 第二期的流程骨架可照抄 |
| `MEDIA_ROOT` / `MEDIA_URL` 設定 | `config/settings.py` | 本機開發用，R2 上線後仍保留 |
| `STORAGES` 設定區塊 | `config/settings.py` | 第一期要在這裡加 R2 backend |
| 現成的 upload 欄位範例 | `accounts.User.avatar`、`nutrition.MealLog.photo` | model 寫法參照 |
| `Pillow` 已安裝 | `requirements.txt` | 縮圖用 |
| `anthropic` 已安裝 | `requirements.txt` | 第二期用 |

### 3.4 動工前的三個問題（2026-09-10 已決定）

- [x] **R2 還是 S3？** → R2。理由見決策紀錄。
- [x] **影片掛在哪個現有實體下？** → **獨立影片庫＋選擇性關聯**。`TrainingVideo`
      只硬性要求 `athlete` 與 `date`；`record`（`analytics.MetricRecord`）與
      `activity`（`training.SessionActivity`）兩個 FK 都可以留空。
      理由：拍片的當下未必已經登了數據，硬綁會讓人乾脆不傳。
- [x] **新開一個 app 還是掛進 `training`？** → 新開 `video` app。
      影片有自己的儲存、保留期限與批註，跟課表是兩件事。
- [x] **儲存怎麼切換？** → 環境變數。設了 `R2_BUCKET` 等四個變數就走 R2 直傳，
      沒設就存本機 `MEDIA_ROOT`。本機開發不用開 Cloudflare 帳號。

### 3.5 第一期做出來的東西（2026-09-10）

| 檔案 | 作用 |
|---|---|
| `video/models.py` | `TrainingVideo`（含 `remote_key` / `file` 兩條儲存路徑、`is_keeper`）、`VideoNote`（`at_sec` 綁時間點、`end_sec` 綁一段區間、`data` 存量測結果） |
| `video/services.py` | 可見範圍、上傳驗證、選擇性關聯、紀錄權限、量測資料的清洗（`_clean_data` / `_clean_shape`） |
| `video/storage.py` | R2 presigned PUT / GET；沒設定就回 `None`，呼叫端自動退回一般表單上傳 |
| `video/management/commands/purge_videos.py` | 保留期限清理，預設 90 天，`--apply` 才真的刪 |
| `video/management/commands/check_r2.py` | R2 連線／寫入／CORS 的自我檢查，上傳出問題先跑這個 |
| `video/migrations/0002_...py` | `VideoNote` 加 `end_sec` 與 `data` |
| `core/views.py` | `video_list` / `video_detail` / `video_sign` |
| `templates/web/video.html`、`video_detail.html` | 影片庫、播放器＋分析工具＋紀錄＋並排比對 |
| `static/js/video.js` | 瀏覽器讀片長尺寸、canvas 擷封面、播不動時提醒、直傳進度、逐格與變速、放大平移、三個分析工具 |
| `video/tests.py` | 44 個測試：可見範圍、上傳驗證、量測資料清洗、保留期限、view 路徑 |

**用詞**：介面上叫「分析重點紀錄」，資料庫的 model 仍叫 `VideoNote`
（改名要動 migration，不值得）。看程式時兩個是同一件事。

### 3.6 分析工具與放大（2026-09-10 追加）

全部在瀏覽器裡完成，量出來的東西按「加入分析重點紀錄」寫進 `VideoNote`，
運動員自己打開就看得到教練量了什麼、劃了什麼，不用另外傳圖。

| 工具 | 做什麼 | 存進 `data` 的內容 |
|---|---|---|
| 計時 | 標 A、標 B（或按 A / B 鍵），量中間那一段；填了距離就一併算平均速度 | `{"kind": "timing", "distance_m": 30}`，區間本身存在 `at_sec` / `end_sec` |
| 數步 | 腳落地按一下（空白鍵），算步數、步頻、每步平均 | `{"kind": "steps", "taps": [...]}` |
| 放大 | ＋／−／還原（鍵盤 + - 0），按著 Ctrl 滾滾輪或觸控板捏合以游標為中心縮放；放大後拖曳可移動畫面 | 不存，純看片用 |
| 劃線 | 疊在影片上的 canvas：直線、三點量角度、水平線、垂直線、手繪 | `{"kind": "draw", "shapes": [{"t": "line", "p": [[x, y], ...]}]}` |

兩個要注意的地方：

- **準確度受格率限制。** 30fps 一格 0.033 秒，逐格只能停在格上，所以量出來的
  時間誤差約在正負一格之內。要量觸地時間這種等級的東西，得用手機的慢動作
  （120 / 240fps）拍，並在播放列的「格率」選對——那個選項只是告訴前端一格多久，
  不會改變影片本身。
- **座標一律存 0～1 的比例，不是像素。** 同一條線在手機、電腦、不同解析度的影片上
  才會畫在同一個位置。`data` 整包是前端送上來的，等於使用者可以隨便塞，
  所以 `video/services.py` 的 `_clean_data` / `_clean_shape` 會把形狀種類、座標範圍、
  筆數（400 步 / 60 個形狀 / 一筆 300 點）、NaN 與 inf 全部擋掉再存。

`data` 用一個 JSONField 而不是拆成好幾欄，是為了第二期：關節角度、
桿路那些數字可以直接放進同一個欄位，不用再開一次 migration。

刻意不做的事：後端 ffmpeg 轉檔（HEVC 改用前端偵測＋提醒使用者改設定）、
任何 AI 分析（那是第二期）。

### 3.7 上線流程（2026-09-10 實際做過一次）

換機器、換 bucket、或哪天要重來一次的話，照這個順序：

1. **Cloudflare → R2 → 開一個 bucket**（正式站用的那個）。
2. **R2 → Manage API Tokens → Create API Token**，權限要選
   **Object Read & Write**（只給 Read 的話上傳會失敗，`check_r2` 會講）。
   記下 Access Key ID 與 Secret Access Key，Secret 只會顯示一次。
3. **Render → 你的 service → Environment**，加四個變數：
   `R2_BUCKET`、`R2_ACCOUNT_ID`、`R2_ACCESS_KEY_ID`、`R2_SECRET_ACCESS_KEY`。
   （`R2_ENDPOINT` 與 `R2_PUBLIC_BASE` 可留空；沒設 public base 就用簽發的
   6 小時 presigned GET 播放。）
   **貼的時候不要多按 Enter**——原因見 3.8。
4. **R2 → bucket → Settings → CORS Policy**，貼上（網址換成你自己的）：

   ```json
   [
     {
       "AllowedOrigins": ["https://www.hohosports.com", "https://hohosports.com"],
       "AllowedMethods": ["PUT", "GET", "HEAD"],
       "AllowedHeaders": ["content-type"],
       "MaxAgeSeconds": 3600
     }
   ]
   ```

   這一步漏掉的話，前端直傳會 100% 失敗而且錯誤訊息很不明顯。
   用 onrender.com 的網址測就要把那個網域也加進去。
5. **部署**。`build.sh` 裡已經有 `migrate --no-input`，migration 會自己跑，
   不用手動進 shell。
6. **驗收**：Render → Shell → `python manage.py check_r2`。
   它會依序檢查連線、寫入、刪除、CORS，每一步都印結果。

設定沒設齊時整套會自動退回本機 `MEDIA_ROOT` 的一般表單上傳，
所以本機開發不必開 Cloudflare 帳號。

### 3.8 上線時踩到的坑（都修好了，但值得記住）

| 症狀 | 真正的原因 | 怎麼查出來的 |
|---|---|---|
| 「上傳失敗：連不上雲端儲存。」 | bucket 根本沒設 CORS | 從外面對 R2 endpoint 送一個 `curl -X OPTIONS`，回 `403 CORS not configured` |
| CORS 修好之後改成 `Server Error (500)` | `R2_ACCESS_KEY_ID` 的值裡夾了一個換行（在 Render 貼的時候多按了 Enter）。金鑰要算進 AWS4 簽章，帶著換行 Python 的 `http.client` 直接拒發整個請求，錯誤訊息是 `Invalid header value`，完全看不出是空白字元 | Render Shell 裡跑 `default_storage.save()` 一行，拿到完整 traceback |
| 上面那個坑為什麼躲過了 `check_r2` | `video/storage.py` 讀環境變數時有 `strip()`，`config/settings.py` 沒有。**同一份設定有兩個讀法**，所以簽發網址與自我檢查都正常，只有 django-storages 那條路壞掉 | 兩邊都改成 `_r2_env()` 統一 strip，`check_r2` 也加了空白字元的警告 |
| 伺服器端 500MB 上限形同虛設 | `video_sign` 讀 `request.POST["size"]`，但前端送的欄位叫 `size_bytes` | 寫測試時發現 |
| `video_detail` 未登入也進得去 | 新加的 helper 放在 `@login_required` 和 view 中間，把那個 decorator 接走了 | 測試 client 打過去炸在 `'str' object has no attribute 'user'` |
| 測試報 `Missing staticfiles manifest entry` | `ManifestStaticFilesStorage`：新的 js/css 要先 `collectstatic` | 改前端後記得 `py -3.13 manage.py collectstatic --noinput` |
| `purge_videos` 的測試無故失敗 | 保留期限是拿**真的今天**算的，測試卻用固定的 `TODAY = 2026-06-01` | 測試改用 `timezone.localdate()` |

兩個環境上的提醒：

- 這台機器要用 **`py -3.13 manage.py ...`**，直接打 `python` 的那個沒裝 Django。
- 改 management command 之後跑一次 `py -3.13 -m py_compile`。
  `manage.py check` **不會**載入 command，語法錯誤它抓不到，測試也一樣過。

---

## 四、第二期：姿態估計 + AI 回饋

### 4.1 做法

用 MediaPipe 或 MoveNet 逐格算骨架關鍵點 → 得到每一格的關節座標 → 導出真實指標（膝角、髖角、槓路軌跡、步頻、觸地時間、左右對稱性）→ 把**數字**（不是圖）交給 Claude 寫成教練語言的回饋。

輸出是骨架疊加影片 + 時間序列曲線 + 文字回饋。這才是專業器材在做的事。

### 4.2 在哪裡跑推論

| | 瀏覽器端（MediaPipe JS） | 後端 |
|---|---|---|
| 成本 | **$0** | Render standard worker 約 US$25/月 |
| 速度 | 現代手機跑 30 秒片只要幾秒 | 30 秒片約需 1–3 分鐘 CPU |
| 影片傳輸 | 不用來回傳 | 要 |
| 缺點 | 結果受裝置影響 | 貴、慢 |

**建議走瀏覽器端。** Render starter 的 CPU 跑逐格推論會很吃力。

### 4.3 技術風險

MediaPipe 吐出來的是座標，**把座標轉成有意義的運動指標本身就是一門學問**，調準確度會反覆很多次。這是第二期真正的難點，不是串接 API。

### 4.4 分析品質的期望值

- **重量訓練**：判斷「深蹲膝內扣、硬舉圓背、槓路偏移、深度不足」這類，可用性不錯。
- **跑步**：定性描述（觸地位置、軀幹前傾、擺臂）OK；步頻、觸地時間這種數字**必須**靠 pose estimation 才算得準。

**務實原則：AI 出定性回饋 + 教練覆核，不要對外宣稱是量測。**

---

## 五、費用

### 5.1 計算假設

50 個運動員 × 每人每週 4 條片 × 每條 30 秒約 30MB
→ 每月約 **800 條 / 24GB**，配 90 天保留政策後常駐約 **72GB**

> 1080p 手機影片約每分鐘 100–130MB；30 秒一組深蹲約 50MB。

### 5.2 第一期（上線後的實際數字）

第一期做完之後，**每月增加的費用是 $0**：

| 項目 | 現在 | 為什麼 |
|---|---|---|
| R2 儲存 | $0 | 前 10GB 免費（約 200 條 30 秒的 1080p 片） |
| R2 流量 | $0 | R2 不收 egress |
| R2 操作次數 | $0 | 免費額度每月 100 萬次寫、1000 萬次讀 |
| 分析工具、放大、逐格 | $0 | 全在瀏覽器算，伺服器只存文字 |
| 轉碼 | $0 | 沒做後端轉碼 |

超過免費額度之後是 **US$0.015/GB/月**：100GB 約 $1.4、500GB 約 $7.4。
照 5.1 的假設（常駐 72GB）大約 $1/月。

**唯一會讓費用跑掉的是片一直存著不刪**，所以 `purge_videos` 的排程是
成本控制的重點，不只是私隱問題。

### 5.3 第二期

| 項目 | 月費（美元） |
|---|---|
| 姿態估計（瀏覽器 MediaPipe） | **$0** |
| 姿態估計（改後端跑） | $25 |
| Claude 生成教練回饋 | $24（Opus 5）／$10（Sonnet 5） |
| **小計** | **$24 – $49** |

每條片的 Claude 成本：約 2k input tokens + 800 output tokens
→ Opus 5（$5/$25 per 1M）約 **$0.03**；Sonnet 5（$2/$10）約 **$0.013**

### 5.4 總計

| | 每月 | 每年 |
|---|---|---|
| 上線前（Render starter + Postgres） | ~$13 | ~$156 |
| **現在（＋第一期＋分析工具）** | **~$13** | **~$156** |
| 影片長到 100GB 都不刪 | ~$14.4 | ~$173 |
| + 第二期 | ~$40 – $70 | ~$480 – $840 |

### 5.5 真正的成本是工時

基礎設施費用小到可以忽略。實際成本是開發量：

- **第一期**實際約 2,600 行，原估 600–800 行。分佈：
  `static/js/video.js` 709、`video/tests.py` 416、`video/services.py` 346、
  `video/models.py` 246、兩個模板 451、`storage.py` 116、兩個 management command 198，
  另加 `core/views.py` 裡的三個 view。多出來的主要是分析工具、放大，
  以及那些「使用者亂塞什麼都不能壞」的防呆與測試
- **第二期**約 800–1200 行，且技術風險高得多

> ⚠️ R2 與 Render 的價格為引用自記憶的估算，數量級可靠但**下單前請自行核對官網**。
> Anthropic API 價格（Opus 5 $5/$25、Sonnet 5 $2/$10 每百萬 token）為準確值。

---

## 六、私隱與保留政策

這是 DBS 學生的影片，保留期限與存取權限遲早要向學校交代，**不如一開始就設計進去**：

- 建議 90 天後自動刪除，教練標記為「範本」的永久保留
- 存取權限：運動員只看得到自己的片；教練看得到自己帶的運動員
- 上傳前需要家長／學生同意的流程（待確認學校要求）

保留政策同時解決成本與私隱兩個問題。

**現況**：`is_keeper`（範本）與 `purge_videos` 都做好了，但
**清理還沒有自動排程**，現在要手動跑：

```
python manage.py purge_videos --days 90          # 只列出會刪什麼
python manage.py purge_videos --days 90 --apply  # 真的刪
```

排程有兩條路：Render 的 Cron Job（另外一個小服務，約 $1/月），
或每學期手動跑一次（$0）。上傳量還小的時候手動就夠。

---

## 七、決策紀錄

| 日期 | 決策 | 理由 |
|---|---|---|
| 2026-09-10 | 排除「抽格丟給 Claude」方案 | 產品觀感不專業，且成本比 pose estimation 高 4–5 倍 |
| 2026-09-10 | 分兩期，第一期不含 AI | 先驗證教練是否真的需要自動分析，再投入第二期的工時與月費 |
| 2026-09-10 | 儲存用 Cloudflare R2 而非 Render Disk | 免 egress 費用；Render Disk 會鎖死單一 instance 並失去 zero-downtime deploy |
| 2026-09-10 | 上傳走 presigned URL 前端直傳 | gunicorn `--timeout 120`，大檔會超時並佔住 worker 記憶體 |
| 2026-09-10 | 獨立影片庫，關聯做成選填 | 拍片時未必已登數據；硬綁會讓人乾脆不傳 |
| 2026-09-10 | 儲存用環境變數切換，本機優先 | 不必先開 Cloudflare 帳號就能把第一期跑起來測 |
| 2026-09-10 | 片長／尺寸／封面都在瀏覽器算 | 免掉伺服器裝 ffmpeg，Render starter 也扛得住 |
| 2026-09-10 | 放大用 CSS transform 包住影片＋畫布一起縮放 | 劃的線才會跟影片一起放大、不會錯開；也不必重新編碼或另存一份放大的片 |
| 2026-09-10 | 分析工具的結果存進批註，不做成即用即棄 | 教練量完的數字要留得住才有意義；順便讓運動員自己看得到 |
| 2026-09-10 | 量測結果用一個 `data` JSONField | 第二期的關節角度可以沿用同一欄，不必再開 migration |
| 2026-09-10 | 劃線座標存 0～1 比例並在後端夾範圍 | 跨裝置位置才一致；前端送來的東西一律當作不可信 |
| 2026-09-10 | 一開始就有 `is_keeper` 與 `purge_videos` | 保留政策同時管住儲存費用與學生影片的私隱，事後補要改 migration |
| 2026-09-10 | 環境變數一律 `strip()`，且只留一個讀法 | 在 Render 貼金鑰很容易多帶一個換行；兩個讀法會讓症狀只出現在其中一條路上，極難查 |
| 2026-09-10 | 介面用「分析重點紀錄」，model 名稱不動 | 改 model 名要動 migration 與所有引用，換不到等值的好處 |
| 2026-09-10 | 紀錄欄從右側窄欄移到播放器下方整行 | 紀錄常常是一整段字，320px 的欄位讀起來很擠 |

---

## 八、日後要做的事

按「值不值得現在做」排序：

1. **`purge_videos` 排程**（見第六節）。上傳量還小可以先手動，
   但這是唯一會讓費用與私隱風險同時長大的東西。
2. **上傳成功率看不到**。失敗的上傳不會在資料庫留任何痕跡，
   所以「有沒有人傳到一半失敗」目前查不出來。要知道的話，得把
   `video_sign` 發出去的簽章記一筆，再跟事後回報的成功配對。
   等有人反映傳不上去再做。
3. **並排比對時第二條片不能放大**。兩邊各自放大再對位其實更難看，
   要做的話應該是「兩片一起放大、一起平移」，不是各放各的。
4. **第二期：姿態估計 + AI 回饋**（第四節）。動工前先看一個學期的真實使用：
   教練如果靠手動計時、數步、劃線就夠用，第二期可以不做。
   要做的話，`VideoNote.data` 這個 JSONField 已經預留給關節角度，
   不用再開 migration。

動這一塊之前建議先讀：3.6（量測資料的格式與清洗）、3.7（部署）、3.8（踩過的坑）。
